meta {
  name: List all Practitioners
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/fhir/Practitioner
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Returns a Bundle containing all Practitioner resources in the system.
}
