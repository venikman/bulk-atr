meta {
  name: Read Encounter by ID
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/fhir/Encounter/{{encounterId}}
  body: none
  auth: none
}

tests {
  test("Encounter read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Encounter");
  });
}

docs {
  Read a single Encounter resource by ID.
}
