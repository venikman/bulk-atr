meta {
  name: Read Organization by ID
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/fhir/Organization/{{organizationId}}
  body: none
  auth: none
}

tests {
  test("Organization read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Organization");
  });
}

docs {
  Read a single Organization resource by ID.
}
