meta {
  name: List all PractitionerRoles
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/fhir/PractitionerRole
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Returns a Bundle containing all PractitionerRole resources in the system.
}
