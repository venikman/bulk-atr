meta {
  name: List All Observations
  type: http
  seq: 21
}

get {
  url: {{baseUrl}}/fhir/Observation
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Returns a Bundle containing all Observation resources.
}
