meta {
  name: Read Coverage by ID
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/fhir/Coverage/{{coverageId}}
  body: none
  auth: none
}

tests {
  test("Coverage read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Coverage");
  });
}

docs {
  Read a single Coverage resource by ID.
}
