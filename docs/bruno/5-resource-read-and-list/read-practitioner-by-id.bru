meta {
  name: Read Practitioner by ID
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/fhir/Practitioner/{{practitionerId}}
  body: none
  auth: none
}

tests {
  test("Practitioner read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Practitioner");
  });
}

docs {
  Read a single Practitioner resource by ID.
}
