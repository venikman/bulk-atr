meta {
  name: Read Patient by ID
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/fhir/Patient/{{patientId}}
  body: none
  auth: none
}

tests {
  test("Patient read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Patient");
  });
}

docs {
  Read a single Patient resource by ID.
}
