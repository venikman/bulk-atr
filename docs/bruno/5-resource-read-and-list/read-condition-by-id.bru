meta {
  name: Read Condition by ID
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/fhir/Condition/{{conditionId}}
  body: none
  auth: none
}

tests {
  test("Condition read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Condition");
  });
}

docs {
  Read a single Condition resource by ID.
}
