meta {
  name: Read Location by ID
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/fhir/Location/{{locationId}}
  body: none
  auth: none
}

tests {
  test("Location read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Location");
  });
}

docs {
  Read a single Location resource by ID.
}
