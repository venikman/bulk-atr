meta {
  name: Read MedicationRequest by ID
  type: http
  seq: 24
}

get {
  url: {{baseUrl}}/fhir/MedicationRequest/{{medicationRequestId}}
  body: none
  auth: none
}

tests {
  test("MedicationRequest read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("MedicationRequest");
  });
}

docs {
  Read a single MedicationRequest resource by ID.
}
