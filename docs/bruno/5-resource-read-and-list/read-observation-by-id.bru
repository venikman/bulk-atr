meta {
  name: Read Observation by ID
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/fhir/Observation/{{observationId}}
  body: none
  auth: none
}

tests {
  test("Observation read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Observation");
  });
}

docs {
  Read a single Observation resource by ID.
}
