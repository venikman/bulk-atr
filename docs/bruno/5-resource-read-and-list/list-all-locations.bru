meta {
  name: List all Locations
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/fhir/Location
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Returns a Bundle containing all Location resources in the system.
}
