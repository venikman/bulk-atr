meta {
  name: Read RelatedPerson by ID
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/fhir/RelatedPerson/{{relatedPersonId}}
  body: none
  auth: none
}

tests {
  test("RelatedPerson read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("RelatedPerson");
  });
}

docs {
  Read a single RelatedPerson resource by ID.
}
