meta {
  name: Read Procedure by ID
  type: http
  seq: 20
}

get {
  url: {{baseUrl}}/fhir/Procedure/{{procedureId}}
  body: none
  auth: none
}

tests {
  test("Procedure read succeeds", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Procedure");
  });
}

docs {
  Read a single Procedure resource by ID.
}
