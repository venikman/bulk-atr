meta {
  name: Bulk status — job not found
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/fhir/bulk-status/00000000-0000-0000-0000-000000000000
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Returns 404 for unknown job", function() {
    expect(res.status).to.equal(404);
  });
}

docs {
  Polling a non-existent or expired job returns 404.
}
