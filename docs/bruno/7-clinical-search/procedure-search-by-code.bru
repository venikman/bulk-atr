meta {
  name: Procedure Search by Code
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/fhir/Procedure?code=http://www.ama-assn.org/go/cpt|99385
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search procedures by CPT code.
}
