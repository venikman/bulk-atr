meta {
  name: Patient Search by Gender
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/fhir/Patient?gender=female
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search patients by administrative gender.
}
