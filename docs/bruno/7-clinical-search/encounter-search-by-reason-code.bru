meta {
  name: Encounter Search by Reason Code
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/fhir/Encounter?reason-code=http://hl7.org/fhir/sid/icd-10-cm|E11.9
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search encounters by ICD-10 reason code.
}
