meta {
  name: Encounter Search by Type
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/fhir/Encounter?type=http://www.ama-assn.org/go/cpt|99213
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search encounters by CPT type code.
}
