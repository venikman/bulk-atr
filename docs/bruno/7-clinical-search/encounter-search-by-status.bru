meta {
  name: Encounter Search by Status
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/fhir/Encounter?status=finished
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search encounters by status (planned, arrived, triaged, in-progress, onleave, finished, cancelled).
}
