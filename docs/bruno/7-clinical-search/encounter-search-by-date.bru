meta {
  name: Encounter Search by Date
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/fhir/Encounter?date=ge2025-01-01&date=le2025-12-31
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search encounters by date range using ge/le prefixes.
}
