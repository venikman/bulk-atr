meta {
  name: Patient Search by Name
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/fhir/Patient?name=Smith
  body: none
  auth: none
}

tests {
  test("Returns a searchset Bundle", function() {
    expect(res.status).to.equal(200);
    expect(res.body.resourceType).to.equal("Bundle");
    expect(res.body.type).to.equal("searchset");
  });
}

docs {
  Search patients by name (partial match across family and given names).
}
